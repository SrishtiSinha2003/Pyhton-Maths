import numpy as np
A = np.array([[1, 2, -1],
              [-2, 2, 3],
              [2, 0, 4]])

B = np.array([[6],
              [3],
              [4]])

# Construct the augmented matrix AB
AB = np.hstack((A, B))

# Print the augmented matrix
print("Augmented matrix AB:")
print(AB)